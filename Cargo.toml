[package]
name = "runic"
version = "0.5.0"
authors = ["Andrew <andrew.pa@outlook.com>"]

[dependencies]
winit = "0.7"

[target.'cfg(windows)'.dependencies]
winapi = "0.2"
user32-sys = "0.1"
kernel32-sys = "0.2"
